(* valid program which tests recursive type definitions *)

type intlist = 
  | Cons int * intlist
  | Nil

and inttree =
  | Empty
  | Node of tree * int * tree


let lis: intlist = Cons 0 Empty in
lis