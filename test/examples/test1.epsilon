(* valid program which tests lists *)

(* [make_list n init] creates a new list of ints of size [n] and with
  initial value [n init].  *)
let make_list = \(n: int) (init: int) -> {
  if n = 0 then
    []
  else
    init :: (make_list (n - 1) init)
}

make_list 10 0