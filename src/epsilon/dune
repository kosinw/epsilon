(ocamllex lexer)

(menhir
 (modules parser)
 (flags --table --dump --inspection --explain --unused-tokens))

(library
 (name epsilon)
 (public_name epsilon)
 (preprocess
  (pps ppx_deriving.show))
 (libraries nice_parser menhirLib))
