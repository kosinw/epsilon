(ocamllex lexer)

(menhir
 (modules parser)
 (flags --explain --unused-tokens)
 (infer true))

(library
 (name epsilon)
 (public_name epsilon)
 (preprocess
  (staged_pps ppx_import ppx_deriving.show))
 (libraries nice_parser base))
