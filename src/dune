(ocamllex lexer)

(menhir
 (modules parser))

(library
 (name epsilon)
 (modules lexer parser epsilon)
 (public_name epsilon)
 (preprocess
  (staged_pps ppx_import ppx_deriving.show))
 (libraries base))
