(ocamllex lexer)

(menhir
 (modules parser)
 (flags --table --unused-tokens))

(library
 (name epsilon)
 (public_name epsilon)
 (preprocess
  (staged_pps ppx_import ppx_deriving.show))
 (libraries menhirLib base))
